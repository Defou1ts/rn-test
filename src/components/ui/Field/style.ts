import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	input: {
		borderRadius: 16,
		backgroundColor: '#e7e7e7',
		marginTop: 16,
		padding: 16,
		width: '100%',
		fontSize: 20,
	},
});
